"""Evidence-driven search-opportunity discovery pipeline.

Every record keeps its provenance: which source produced it, when, and from
which seed. Search volumes are stored per provider and never invented; a
missing volume is a missing volume. Community counts are stored as their own
metric kinds and are never added to search volume.
"""
