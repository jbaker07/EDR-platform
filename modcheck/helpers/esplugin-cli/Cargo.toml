[package]
name = "modcheck-esplugin"
version = "0.1.0"
edition = "2021"
description = "Thin JSON wrapper around esplugin, the plugin parser LOOT uses."
license = "GPL-3.0-or-later"

[dependencies]
esplugin = "6.1"
serde_json = "1"

[[bin]]
name = "modcheck-esplugin"
path = "src/main.rs"
