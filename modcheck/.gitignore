.venv/
__pycache__/
*.pyc
.pytest_cache/
# Cached upstream evidence: large, re-fetchable, and often not redistributable.
# Provenance (URL, sha256, retrieved_at) is committed in packs/*/sources.yaml.
evidence_cache/
build_workspaces/
*.egg-info/
# Optional Rust helper build output (GPL-3.0 binary, built on demand)
helpers/*/target/
# Reference implementations installed on demand for cross-validation
helpers/*/node_modules/
helpers/*/package-lock.json
.cache/
atlas/harness/mixin_transform/build/
