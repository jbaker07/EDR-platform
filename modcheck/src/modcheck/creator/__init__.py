"""The creator path: project setup, reviewable changes, builds and release reports.

Nothing is re-exported here on purpose: a name like ``scaffold`` re-exported
from the package would shadow the ``creator.scaffold`` module of the same name.
Import from the submodules directly.
"""
